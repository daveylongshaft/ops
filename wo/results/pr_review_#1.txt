This PR is incomplete.

- The PR description claims to port modules identified as MISSING, but several files I looked for are not present (e.g., `persistent_clients.py`, `server_s2s.py`).
- The PR description also claims that tests were written for each ported module, but I could not find any tests for `APIKeyManager`.
- The `api_key_manager.py` module has a potential race condition in `save_config` as the read-modify-write operation is not atomic.

Please add the missing files and tests before this PR can be approved.
