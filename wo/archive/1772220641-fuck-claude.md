--- Work Log ---
PID: 985 agent: opus starting at 2026-02-26 19:11
reading queue_worker.py, agent_service.py, run_agent.py, agent dirs
understanding current system flow and identifying gaps
implementing: 1) add missing run_cycle() to queue_worker.py
implementing: 2) fix spawn_agent to pass relative orders.md path for temp repo
implementing: 3) create cagent-based run_agent scripts for local agents (codellama, deepseek, qwen, dmr_qwen_task_runner)
implementing: 4) update template run_agent.py to handle relative workorder paths
implementing: 5) update KNOWN_AGENTS to include all agent dirs
implementing: 6) checking csc-ctl issue
verified queue_worker.py syntax: OK
verified csc-ctl status/show/dump/restart/install commands: all working

Summary of changes:
1. queue_worker.py: Added missing run_cycle() function that main() calls
2. queue_worker.py: Added missing ACTIVE_PROCS dict for Popen tracking
3. queue_worker.py: Updated KNOWN_AGENTS to include all agents (sonnet, gemini-2.5-pro, previews, dmr_qwen_task_runner, ollama-*, test-agent)
4. queue_worker.py: Fixed spawn_agent to pass RELATIVE path (agents/<name>/queue/work/orders.md) instead of absolute main-repo path - works correctly in temp repo cwd
5. queue_worker.py: spawn_agent now falls back to template run_agent scripts if agent-specific not found
6. queue_worker.py: Fixed git add to use dynamic prompts dir name (workorders/ or prompts/)
7. Created cagent-based run_agent.sh + run_agent.bat for local agents: codellama, deepseek, qwen, dmr_qwen_task_runner (each uses cagent exec with their own cagent.yaml)

Flow verification (matches workorder spec):
- PM assigns workorder -> moves to wip, generates orders.md in queue/in
- Queue worker picks up orders.md from queue/in -> moves to queue/work
- Queue worker git add+commit+push (stages agents/ + workorders/)
- Queue worker git pull in agent's temp repo (gets orders.md + WIP file)
- Queue worker spawns agent with cwd=temp_repo, relative path to orders.md
- Agent works in temp repo, journals to workorders/wip/<filename>
- When agent finishes: commit+push from temp repo, pull to main repo
- Queue worker checks COMPLETE marker -> moves to done/ or back to ready/
COMPLETE
