

run_agent.bat is used for non-local agents, cagent is used for local, codellame deepseek and qwen and dmr_qwen_task_runner none of them should have been changed.  the
  individual agents have their own setup for a reason, if i wanted the same script in every dir i would just use the one in template where it sits

the agent is looking for the relative path which is good except that the relative path is for its repo in the temp dir, noot the main repo, we need the run_agent script
  to copy the wip and orders.md from the main local copy to the temp repo the agent uses by the queue-worker script after repo is setup but before the agent is run.

make sure the queue worker is also copying the orders.md and the wip file into the agents repo in the temp dir, just before it spawns the agent, and then when the agent
  finishes, copy them back to the main copy c:\csc\agents\*\queue\out and the workorder from the  temp repo workorders\wip\filename.md to the c:\csc\ repo before it does
  its cleanup and map update commit/push so the scripts can process the work in the main repo

 queue worker picks up orders.md and actual workorder\wip\file.md and COPIES them to the agents temp repo so he can find them, alternately before calling the agent, after
  the files are in work / wip you would have to commit/push to github and then pull the agents copy of the repo with the files in place before running the agent

the queue worker has to git commit and git push after the files are in place but before the agents repo is updated with git pull

 the workorder stays in workorders\wip\ it does not get moved to the queue\work dir  the queue\in|work|out is used to serialize running agents and to give them all the
  exact same context for working the workorder, it tells them where to find the workorder in their working copy and where to find all the tools and information they need
  and how to do the actual work described in the workorder. do not get workorders which are TASK instructions that vary widely -vs- orders.md which is SYSTEM policies and
  procedures of how to do the work which is described in the orders.md templates so that there is consistency in how the work is done and accounted for, audited, verified,
  logged

 pm executes the agent service modules select method to choose a model, then excutes the assign method to give the workorder to the agent selected.  the assign method
  moved the wo from ready to wip so no one else tries to grab it, thats why it must be in ready to be assigned,  and then the assign method also generates the orders.md
  from template and places it in the queue/in dir indicating to the queue worker that there are orders ready to be carried out.  that queue worker if there are no other
  files in any queue/work dir, will move the first one it finds in queue\in to queue\work, and then with the wo in wip and the orders.md in work, will git add . and git
  commit -a with message "assigning task <workorder name> to <agent_name>", and git push the commit and verify it succeded and completed.  with the files pushed to github
  the queue-worker will then execute git pull in the agents repo which will download the current working version including the files that are being assigned.  when the
  files are in the temp repo, then the queue-worker can run_agent script  which will cd to the temp repo and execute the run_agent script from the agents\agent\dir (or
  agents\template\ fallback) to work in yolo mode as a 1-shot run against a written prompt non-interactively and will log the output.  when the queue-worker runs next, it
  will check the pid of the work in progress and will do its git commit and push the changes and after they are pushed will then pull them to the main repo so the scripts
  can continue processing them.  are there any gaps in that?  make that happen, an record it in a workorder so we dont forget but do the work manually but keep track with a
  workorder file



--- Agent Log ---
You've hit your limit · resets 7pm (America/Chicago)
[run_agent] Agent: opus, Root: C:\csc
[run_agent] Starting Claude (claude-opus-4-6) for opus


--- Verify/Complete or Finish ---
Please verify this workorder is complete or finish the work and add COMPLETE as the last line.

INCOMPLETE: Agent task did not finish properly (missing COMPLETE marker)


DEAD END - Stale test/reference file
