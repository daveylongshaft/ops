Reading test_agent_service — tests use old agent names 'claude'/'gemini', need to match KNOWN_AGENTS keys
Fixed tests to use model-specific agent names (sonnet instead of claude)
commit and move to done
